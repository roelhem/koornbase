query getAppsForTable($page:Int = 0, $perPage:Int = 10) {
    apps(limit:$perPage, page:$page) {
        from
        to
        total
        data {
            id
            name
            name_short
            description
        }
    }
}